---
import tests from "../../config/tests.json";
import AcademyCapIcon from "../icons/academyCap.astro";
---

<section
    class="py-16 px-4 bg-gradient-to-br from-sky-950 to-lightgreen rounded-3xl shadow-xl max-w-4xl mx-auto my-10 text-center text-white drop-shadow-2xl drop-shadow-black"
>
    <h2 class="text-4xl font-bold mb-4">
        ¿Estás listo para poner a prueba tu inglés?
    </h2>
    <p class="mb-10 text-lg">Elegí el nivel que más se ajuste a vos:</p>

    <div class="flex flex-col md:flex-row gap-6 justify-center">
        <!-- Test Básico -->
        {
            tests.map((test) => (
                <a
                    href={test.link}
                    class="group relative block bg-white text-lightgreen rounded-xl p-6 w-full md:w-1/2 shadow-md hover:scale-105 hover:shadow-lg transition-all duration-300"
                >
                    <h3 class="text-2xl font-semibold mb-2 group-hover:text-lightergreen transition-colors">
                        {test.title}
                    </h3>
                    <p class="text-sm text-gray-600 group-hover:text-gray-800">
                        {test.description}
                    </p>
                    <div class="absolute right-5 bottom-5 text-lightergreen group-hover:translate-x-1 transition-transform text-xl">
                        →
                    </div>
                </a>
            ))
        }
    </div>
</section>
